
#include <iostream>
#include <locale.h>
using namespace std;
int main(){
    int resposta;
    setlocale(LC_ALL, "Portuguese");
    cout << "Digite o numero do jogo em que deseja participar sendo:" << endl <<"[1]Mega-Sena" << endl << "[2]Quina" << endl << "[3]Lotomania" << endl << "[4]Lotofácil" << endl;
    cin >> resposta;
    if(resposta == 1){
            int geracao, aposta;
        cout << "Quantas apostas voce deseja gerar?" << endl;
        cin >> geracao;
    do{
        cout << "Digite somente números positivos" << endl;
        cin >> geracao;
    }while(geracao < 1);
        cout << "Na Mega-Sena voce pode apostar entre 6 e 15 dezenas " << endl << "sendo assim, quantas dezenas voce deseja apostar?:" << endl;
        cin >> aposta;
        do{
            cout << "Digite um numero de dezenas valido:" << endl;
            cin >> aposta;
        }while(aposta < 6 || aposta > 15);

    }

    return 0;
